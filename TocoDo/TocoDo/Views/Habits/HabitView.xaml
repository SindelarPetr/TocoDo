<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:converters="clr-namespace:TocoDo.Converters;assembly=TocoDo"
			 xmlns:habits="clr-namespace:TocoDo.Converters.Habits;assembly=TocoDo"
			 xmlns:customRenderers="clr-namespace:TocoDo.CustomRenderers;assembly=TocoDo"
			 x:Class="TocoDo.Views.Habits.HabitView"
			 x:Name="This"
			 x:Key="This">
	<ContentView.Resources>
		<ResourceDictionary>
			<converters:BoolReverseConverter x:Key="BoolReverseConverter"/>
			<habits:HabitDateConverter x:Key="HabitDateConverter"/>
			<converters:IsNotNullConverter x:Key="IsNotNullConverter"/>
		</ResourceDictionary>
	</ContentView.Resources>

	<ContentView.Content>
		<StackLayout Spacing="0" Padding="0, 3">
			<StackLayout.GestureRecognizers>
				<TapGestureRecognizer Command="{Binding EditCommand}" />
			</StackLayout.GestureRecognizers>

			<customRenderers:NoLineEntry x:Name="EntryEditTitle" Text="{Binding ModelTitle, Mode=OneWay}" IsVisible="{Binding IsEditTitleMode}" Unfocused="EditTitle_OnUnfocused" FontSize="Medium" TextColor="{StaticResource ColorPrimaryLight}" />
			<Label Text="{Binding ModelTitle}" IsVisible="{Binding IsEditTitleMode, Converter={StaticResource BoolReverseConverter}}" FontSize="Medium"  TextColor="{StaticResource ColorPrimaryLight}" />
			<Label Text="{Binding ., Converter={StaticResource HabitDateConverter}}" Margin="0" FontSize="12" IsVisible="{Binding ModelStartDate, Converter={StaticResource IsNotNullConverter}}"/>
		</StackLayout>
	</ContentView.Content>
</ContentView>