<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:converters="clr-namespace:TocoDo.Converters;assembly=TocoDo"
			 x:Class="TocoDo.Views.Habits.HabitView"
			 x:Name="This"
			 x:Key="This">
	<ContentView.Resources>
		<ResourceDictionary>
			<converters:BoolReverseConverter x:Key="BoolReverseConverter"/>
		</ResourceDictionary>
	</ContentView.Resources>
	<ContentView.Content>
		<StackLayout Spacing="0">
			<StackLayout.GestureRecognizers>
				<TapGestureRecognizer Command="{Binding EditCommand}" />
			</StackLayout.GestureRecognizers>
			<Entry x:Name="EntryEditTitle" Text="{Binding ModelTitle, Mode=OneWay}" IsVisible="{Binding IsEditTitleMode}" Unfocused="EditTitle_OnUnfocused" FontSize="Large" FontAttributes="Bold" TextColor="Black"/>
			<Label Text="{Binding ModelTitle}" IsVisible="{Binding IsEditTitleMode, Converter={StaticResource BoolReverseConverter}}" FontSize="Large" FontAttributes="Bold" TextColor="Black" />
			<StackLayout Padding="10,0,0,0">
				<Label Text="Starts on 7.10., repeating every day, for 10 days" FontSize="12"/>
				<Label Text="{Binding ModelDescription}" FontSize="Small" />
			</StackLayout>
		</StackLayout>
	</ContentView.Content>
</ContentView>