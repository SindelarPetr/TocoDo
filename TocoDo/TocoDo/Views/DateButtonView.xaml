<?xml version="1.0" encoding="UTF-8"?>
<views:IconButton xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:TocoDo.Views;assembly=TocoDo"
             xmlns:triggerActions="clr-namespace:TocoDo.TriggerActions;assembly=TocoDo"
             xmlns:converters="clr-namespace:TocoDo.Converters;assembly=TocoDo"
             xmlns:controls="clr-namespace:TocoDo.Controls;assembly=TocoDo"
             xmlns:modifyTask="clr-namespace:TocoDo.Converters.ModifyTask;assembly=TocoDo"
             x:Class="TocoDo.Views.DateButtonView"
             x:Name="This">
	<views:IconButton.Resources>
		<ResourceDictionary>
		</ResourceDictionary>
	</views:IconButton.Resources>
	<views:IconButton.Triggers>
		<EventTrigger Event="Clicked">
			<EventTrigger.Actions>
				<triggerActions:FocusAction ElementToFocus="{x:Reference DatePicker}"/>
			</EventTrigger.Actions>
		</EventTrigger>
	</views:IconButton.Triggers>
	<views:IconButton.InnerContent>
		<controls:NullableDatePicker x:Name="DatePicker" IsVisible="False" DateSelected="DatePicker_OnDateSelected" NullableDate="{Binding SelectedDate}"/>
	</views:IconButton.InnerContent>
</views:IconButton>