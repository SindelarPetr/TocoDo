<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:views="clr-namespace:TocoDo.Views;assembly=TocoDo"
			 xmlns:habits="clr-namespace:TocoDo.Views.Habits;assembly=TocoDo"
			 xmlns:services="clr-namespace:TocoDo.Services;assembly=TocoDo"
			 xmlns:abstractions="clr-namespace:Plugin.CrossPlatformTintedImage.Abstractions;assembly=Plugin.CrossPlatformTintedImage.Abstractions"
			 x:Class="TocoDo.Pages.Main.HabitsPage"
			 x:Name="This">
	<ContentPage.Content>
		<ScrollView BindingContext="{x:Reference This}">
			<StackLayout Style="{StaticResource PageStackLayout}">
				<abstractions:TintedImage Style="{StaticResource ButtonAdd}">
					<abstractions:TintedImage.GestureRecognizers>
						<TapGestureRecognizer Tapped="AddButton_OnTapped"/>
					</abstractions:TintedImage.GestureRecognizers>
				</abstractions:TintedImage>
				<!-- Current -->
				<Label Text="Current" Style="{StaticResource FrameTitle}"/>
				<Frame>
					<habits:HabitSetView HabitsSource="{x:Static services:StorageService.CurrentHabits}" />
				</Frame>
				
				<!-- Scheduled -->
				<Label Text="Scheduled" Style="{StaticResource FrameTitle}" />
				<Frame>
					<StackLayout Spacing="6">
					</StackLayout>
				</Frame>

				<!-- Unscheduled -->
						<Label Text="Unscheduled" Style="{StaticResource FrameTitle}" />
				<Frame>
					<habits:HabitSetView x:Name="UnscheduledHabitSetView" HabitsSource="{x:Static services:StorageService.UnscheduledHabits}" />
				</Frame>
					
				<!-- Recommended -->
				<Label Text="Recommended" Style="{StaticResource FrameTitle}" />
				<Frame>
					<StackLayout Spacing="6">
					</StackLayout>
				</Frame>
			</StackLayout>
		</ScrollView>
	</ContentPage.Content>
</ContentPage>